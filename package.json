{
  "name": "mql5-scraper",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npm install && npm run install-chromium && micro api/scrape.js",
    "dev": "npm install && npm run install-chromium && micro api/scrape.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && npm run install-chromium",
    "install-chromium": "npm install puppeteer puppeteer-extra chromium"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@netlify/functions": "^1.6.0",
    "dotenv": "^16.3.1",
    "mongodb": "^5.7.0",
    "puppeteer": "^20.8.1",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "vercel": "^31.0.2"
  },
  "devDependencies": {
    "netlify-cli": "^15.8.1"
  }
}
